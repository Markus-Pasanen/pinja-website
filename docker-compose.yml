services:
  kotisivut:
    build:
      context: .
      dockerfile: Dockerfile
    image: kotisivut:latest
    container_name: kotisivut
    ports:
      - "3000:3000"
    mem_limit: 3g
    restart: unless-stopped
